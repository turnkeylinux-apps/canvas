#!/bin/bash -e

ln -sf /usr/bin/yarnpkg /usr/local/bin/yarn

ruby -e 'abort "Canvas requires Ruby 3.4" unless RUBY_VERSION.start_with?("3.4.")'
node -e 'process.exit(Number(process.versions.node.split(".")[0]) === 20 ? 0 : 1)'
yarn --version | grep -Eq '^1\.(19|2[0-9])\.'
